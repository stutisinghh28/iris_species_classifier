# iris_species_predictor
exploring my way through ai/ml by building basic models to gain some insights

1.What it does: Random Forest classifier predicting iris species from 4 flower measurements



2.How to run: pip install -r requirements.txt then python basic_ml_model.py


3.Sample output: accuracy, confusion matrix, and the predict_species() function usage
