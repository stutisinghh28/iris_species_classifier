# iris_species_predictor
exploring my way through ai/ml by building basic models to gain some insights

What it does: Random Forest classifier predicting iris species from 4 flower measurements
How to run: pip install -r requirements.txt then python basic_ml_model.py
Sample output: accuracy, confusion matrix, and the predict_species() function usage
